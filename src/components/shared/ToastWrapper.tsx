/**
 * ToastWrapper — re-exporta ToastContainer para compatibilidad.
 * @deprecated Usar ToastContainer directamente desde '@/components/shared/Toast'
 */
'use client';
import ToastContainer from './Toast';
export default ToastContainer;
